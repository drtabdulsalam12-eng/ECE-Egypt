# 📧 دليل إعداد EmailJS - خطوة بخطوة

## المشكلة: لا يصل كود التحقق؟

هذا الدليل يساعدك في إعداد EmailJS بشكل صحيح لإرسال أكواد التحقق.

---

## 📋 الخطوات المطلوبة

### 1️⃣ إنشاء حساب EmailJS

1. اذهب إلى [EmailJS.com](https://www.emailjs.com)
2. اضغط على **Sign Up** لإنشاء حساب جديد
3. استخدم بريدك الإلكتروني (يفضل Gmail)
4. قم بتأكيد البريد الإلكتروني

---

### 2️⃣ إضافة Email Service

بعد تسجيل الدخول:

1. اذهب إلى **Email Services** من القائمة الجانبية
2. اضغط على **Add New Service**
3. اختر **Gmail** (أو أي خدمة بريد تستخدمها)
4. اتبع التعليمات لربط حسابك:
   - سيطلب منك تسجيل الدخول بحساب Gmail
   - اسمح بالوصول لـ EmailJS
5. بعد الربط، ستحصل على **Service ID** (احفظه!)
   - مثال: `service_abc123xyz`

**⚠️ مهم جداً:**
- تأكد من عدم تفعيل خيار "Use Default Email Address"
- استخدم بريدك الإلكتروني الفعلي

---

### 3️⃣ إنشاء Email Template

1. اذهب إلى **Email Templates** من القائمة
2. اضغط على **Create New Template**
3. قم بإعداد القالب كالتالي:

#### **Subject (الموضوع):**
```
كود التحقق - ECE Egypt
```

#### **Content (المحتوى):**
```html
مرحباً {{to_name}}،

كود التحقق الخاص بك هو:

{{verification_code}}

هذا الكود صالح لمرة واحدة فقط.

إذا لم تطلب هذا الكود، يرجى تجاهل هذه الرسالة.

مع تحيات،
فريق ECE Egypt
```

#### **To Email:**
```
{{to_email}}
```

4. احفظ القالب
5. ستحصل على **Template ID** (احفظه!)
   - مثال: `template_xyz789abc`

---

### 4️⃣ الحصول على Public Key

1. اذهب إلى **Account** من القائمة (أيقونة المستخدم في الأعلى)
2. اذهب إلى تبويب **API Keys**
3. انسخ **Public Key**
   - مثال: `Ab1cD2eF3gH4iJ5k`

---

### 5️⃣ إضافة المفاتيح إلى Replit Secrets

في Replit:

1. افتح **Secrets** (أيقونة القفل 🔒 في القائمة الجانبية)
2. أضف المفاتيح التالية:

| Key | Value | مثال |
|-----|-------|------|
| `EMAILJS_SERVICE_ID` | Service ID من الخطوة 2 | `service_abc123xyz` |
| `EMAILJS_TEMPLATE_ID` | Template ID من الخطوة 3 | `template_xyz789abc` |
| `EMAILJS_PUBLIC_KEY` | Public Key من الخطوة 4 | `Ab1cD2eF3gH4iJ5k` |

**⚠️ ملاحظة مهمة:**
- تأكد من كتابة الأسماء **بالضبط** كما هي (حساسة لحالة الأحرف)
- لا توجد مسافات إضافية

---

### 6️⃣ التحقق من الإعداد

بعد إضافة المفاتيح:

1. أعد تشغيل السيرفر (سيتم تلقائياً)
2. حاول التسجيل مرة أخرى
3. تحقق من Console في Replit (في الأسفل)
4. ابحث عن:
   - ✅ رسائل خضراء = نجح الإرسال
   - ❌ رسائل حمراء = هناك خطأ (اقرأ التفاصيل)

---

## 🔍 استكشاف الأخطاء

### الخطأ: "Service ID غير صحيح"
- **الحل**: تأكد من نسخ Service ID الصحيح من EmailJS

### الخطأ: "Template ID غير موجود"
- **الحل**: تأكد من إنشاء Template وحفظه في EmailJS

### الخطأ: "Public Key غير صحيح"
- **الحل**: تأكد من نسخ Public Key من Account → API Keys

### لا يصل البريد نهائياً:
1. تحقق من مجلد الـ Spam/Junk في بريدك
2. تأكد من أن الخدمة (Gmail Service) مفعلة في EmailJS
3. تحقق من أن Template يحتوي على `{{to_email}}`

### يصل البريد فارغ:
- تأكد من أن Template يحتوي على المتغيرات:
  - `{{to_name}}`
  - `{{verification_code}}`
  - `{{to_email}}`

---

## 🎯 للاختبار السريع

بعد كل تسجيل، كود التحقق سيظهر في Console في وضع التطوير:

```
🔵 محاولة إرسال البريد الإلكتروني...
📧 إلى: test@example.com
👤 الاسم: محمد أحمد
🔑 كود التحقق: 123456
```

يمكنك استخدام هذا الكود للتحقق حتى لو لم يصل البريد!

---

## 📞 المساعدة

إذا استمرت المشكلة:
1. تحقق من Console في Replit
2. اقرأ رسائل الخطأ بالتفصيل
3. تأكد من جميع الخطوات أعلاه

---

## ✅ Checklist النهائي

- [ ] حساب EmailJS مُنشأ ومُفعل
- [ ] Email Service (Gmail) مُضاف ومربوط
- [ ] Email Template مُنشأ ويحتوي على المتغيرات الصحيحة
- [ ] Public Key منسوخ من Account → API Keys
- [ ] جميع المفاتيح (3) مُضافة في Replit Secrets
- [ ] السيرفر مُعاد تشغيله
- [ ] تم اختبار التسجيل مرة أخرى

---

**بالتوفيق! 🎉**
