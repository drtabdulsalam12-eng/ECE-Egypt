# دليل الإعداد الكامل لـ ECE-Egypt Backend

## ⚠️ خطوة مهمة جداً: إعداد MongoDB Atlas

المشكلة الحالية هي أن قاعدة البيانات **لا تسمح بالوصول من عنوان IP الخاص بـ Replit**. يجب عليك اتباع الخطوات التالية لحل هذه المشكلة:

### الخطوات المطلوبة:

#### 1. تسجيل الدخول إلى MongoDB Atlas
- اذهب إلى [MongoDB Atlas](https://cloud.mongodb.com)
- سجل الدخول بحسابك

#### 2. فتح إعدادات الشبكة (Network Access)
- من القائمة الجانبية، اختر **"Security"** 
- ثم اختر **"Network Access"**

#### 3. إضافة عنوان IP
- اضغط على زر **"ADD IP ADDRESS"** (أخضر اللون)
- ستظهر لك نافذة منبثقة

#### 4. السماح بالوصول من جميع العناوين
اختر أحد الخيارين:

**الخيار الأول (الأسهل):**
- اضغط على **"ALLOW ACCESS FROM ANYWHERE"**
- سيتم إضافة `0.0.0.0/0` تلقائياً

**الخيار الثاني (يدوياً):**
- في خانة **"Access List Entry"** أدخل: `0.0.0.0/0`
- في خانة **"Comment"** اكتب: `Replit Development`

#### 5. تأكيد الإعدادات
- اضغط على زر **"Confirm"**
- انتظر قليلاً حتى تصبح القاعدة نشطة (Active)

### 📸 لقطات شاشة توضيحية:

```
MongoDB Atlas Dashboard
  ↓
Security (القائمة الجانبية)
  ↓
Network Access
  ↓
ADD IP ADDRESS (زر أخضر)
  ↓
ALLOW ACCESS FROM ANYWHERE
  ↓
Confirm
```

---

## 🔐 ملاحظات أمنية مهمة

### لماذا نسمح بـ `0.0.0.0/0`؟
- **Replit لا يوفر عنوان IP ثابت**، لذلك لا يمكننا تحديد عنوان IP محدد
- هذا **آمن للتطوير** لأن الحماية تعتمد على:
  1. **اسم المستخدم وكلمة المرور**: في رابط الاتصال (MONGO_URI)
  2. **Replit Secrets**: جميع البيانات الحساسة مشفرة ومحمية
  3. **عدم مشاركة MONGO_URI**: لا تشاركه مع أحد

### هل هذا آمن؟
✅ **نعم للتطوير والتعلم**  
⚠️ **للإنتاج**: يُفضل استخدام:
- MongoDB Atlas Private Endpoints
- VPC Peering
- أو MongoDB Realm

---

## 🚀 بعد إعداد MongoDB Atlas

بمجرد الانتهاء من السماح بالوصول، سيعمل السيرفر تلقائياً!

### التحقق من عمل السيرفر:

1. انتظر حتى يتم تحديث إعدادات MongoDB (عادة أقل من دقيقة)
2. في Replit، اضغط على زر **Run** أو أعد تشغيل السيرفر
3. يجب أن ترى الرسالة التالية:

```
✅ تم الاتصال بقاعدة البيانات MongoDB بنجاح!
🚀 السيرفر يعمل على المنفذ 5000
```

---

## 📋 التحقق من المتغيرات السرية

تأكد من أن جميع المتغيرات التالية موجودة في **Replit Secrets**:

| المتغير | الوصف | مثال |
|---------|--------|------|
| `MONGO_URI` | رابط MongoDB Atlas | `mongodb+srv://user:pass@cluster.mongodb.net/database` |
| `EMAILJS_SERVICE_ID` | معرف خدمة EmailJS | `service_xxxxxxx` |
| `EMAILJS_TEMPLATE_ID` | معرف قالب EmailJS | `template_xxxxxxx` |
| `EMAILJS_PUBLIC_KEY` | المفتاح العام لـ EmailJS | `xxxxxxxxxxxxxx` |
| `JWT_SECRET` | مفتاح تشفير JWT | `any_long_secure_random_string` |

---

## 🧪 اختبار API

بعد تشغيل السيرفر بنجاح، يمكنك اختبار API:

### 1. اختبار الصفحة الرئيسية
```bash
curl https://your-repl-url.repl.co/api
```

يجب أن يرجع:
```json
{
  "message": "مرحباً بك في ECE-Egypt API",
  "status": "running",
  "version": "1.0.0"
}
```

### 2. تسجيل مستخدم جديد
```bash
curl -X POST https://your-repl-url.repl.co/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "أحمد محمد",
    "nationalId": "12345678901234",
    "email": "ahmed@example.com",
    "phone": "01012345678",
    "password": "password123",
    "governorate": "القاهرة"
  }'
```

---

## ❓ الأسئلة الشائعة

### س: ماذا لو لم يعمل السيرفر بعد إضافة IP؟
**ج:** 
1. تأكد من أن القاعدة أصبحت "Active" (خضراء) في MongoDB Atlas
2. انتظر دقيقة واحدة ثم أعد تشغيل السيرفر
3. تحقق من صحة MONGO_URI في Secrets

### س: هل يجب أن أدفع لـ MongoDB Atlas؟
**ج:** لا، الطبقة المجانية (Free Tier) كافية للتطوير والتعلم.

### س: كيف أحصل على MONGO_URI؟
**ج:**
1. في MongoDB Atlas → اضغط على **"Connect"** بجانب Cluster
2. اختر **"Connect your application"**
3. انسخ Connection String
4. استبدل `<password>` بكلمة المرور الفعلية
5. استبدل `<database-name>` باسم قاعدة البيانات (مثلاً `ece-egypt`)

### س: EmailJS لا يرسل البريد؟
**ج:** تأكد من:
1. صحة Service ID و Template ID و Public Key
2. أن Template يحتوي على `{{verification_code}}`
3. التحقق من مجلد Spam في البريد

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من السجلات (Logs) في Replit
2. راجع ملف README.md
3. تأكد من اتباع جميع الخطوات بالترتيب

---

**تم التطوير بـ ❤️ بواسطة ECE-Egypt Team**  
**تاريخ آخر تحديث**: 27 أكتوبر 2025
